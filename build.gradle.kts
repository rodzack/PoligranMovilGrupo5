// build.gradle.kts (Project)

plugins {
    // Agrega el plugin de gestión de versiones si usas version catalogs
    alias(libs.plugins.kotlinAndroid) apply false
    alias(libs.plugins.androidApplication) apply false
}